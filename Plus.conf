# Diretórios
PLUS_ROOT="/opt/plus"
PLUS_SRC="$PLUS_ROOT/src"
PLUS_LOGS="$PLUS_ROOT/logs"
PLUS_HOOKS="$PLUS_ROOT/hooks"

# Build
BUILD_FLAGS=""
USE_FAKEROOT=true
DESTDIR="$PLUS_ROOT/dest"

# Git
GIT_SYNC=true
GIT_BRANCH="main"

# Logs
LOG_LEVEL="INFO"

# Opcional
FORCE_INSTALL=false

# Checksum
CHECKSUM_VERIFY=true
